#pragma once

// WiFi
#define WIFI_SSID     "YourSSID"
#define WIFI_PASSWORD "YourPassword"

// Display rotation: 1=landscape (USB kanan), 3=landscape (USB kiri), 0/2=portrait
#define DISPLAY_ROTATION 1

// Timezone offset detik dari UTC (WIB = UTC+7 = 25200)
#define NTP_OFFSET_SEC 25200

// MQTT broker (server 88.113.x.x / hostname)
#define MQTT_BROKER    "192.168.x.x"
#define MQTT_PORT      1883
#define MQTT_TOPIC     "claude/usage/total"
#define MQTT_CLIENT_ID "claude-monitor-cyd"

// Budget token — sesuaikan kebutuhan
#define MONTHLY_BUDGET_TOKENS 10000000UL   // 10M token/bulan (Max plan ~200M)
#define WEEKLY_TOKEN_LIMIT    50000000UL   // Tier 1: ~50M/week, Tier 2: ~200M/week
